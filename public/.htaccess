Options -Indexes

RewriteEngine On

# Não redirecionar arquivos/pastas que existem fisicamente
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Tudo vai para index.php (sem RewriteBase para funcionar em subdiretório)
RewriteRule ^(.*)$ index.php [QSA,L]
